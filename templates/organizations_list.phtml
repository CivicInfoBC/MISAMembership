<?php	foreach ($this->results as $x):	?>

<div>

	<a href="<?php
	
		echo(
			htmlspecialchars(
				$request->MakeLink(
					'organization',
					$x->id
				)
			)
		);
	
	?>">
	
		<?php	echo(htmlspecialchars($x->name));	?>
	
	</a>

</div>

<?php	endforeach;	?>